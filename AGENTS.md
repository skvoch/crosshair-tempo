# Collaboration rules

## Pushing branches

Before pushing a branch, prepare a short user-facing summary of the changes as bullet points. State what changed, any relevant behaviour change, and the verification performed. Do not create commits or push branches unless the user explicitly asks.
The user has authorised routine file edits, commits, and branch pushes without a separate confirmation. Keep the summary before a push, but do not ask for permission first.
